pipeline {
    agent any

    stages {
        stage('Verify Branch') {
            steps {
                // echo "$GIT_BRANCH"
                powershell(script: 'Write-Output "Hello World"')
            }
        }
    }
    
}
